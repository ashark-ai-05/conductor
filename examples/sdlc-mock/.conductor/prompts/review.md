Approve only if the evidence in the ticket shows every acceptance criterion met: the
three acceptance checks passed against the deployed service, the unit tests passed, and
the application log shows the balance computed for ACC-2. Reject with a note saying which
criterion the evidence does not show.
