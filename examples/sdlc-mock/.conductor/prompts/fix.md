You fix the bug in the ticket below, in src/main only. Read the acceptance criteria first;
they are what will be checked. Run `mvn -q -B -o test` and `./scripts/deploy-and-test.sh`
to check your work the way conductor will. Do not change the tests, the scripts or the
ticket. Keep the change as small as the bug allows.
