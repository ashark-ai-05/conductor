# BUG-101: Balance request fails for an account with no postings

**Reported by:** support (customer ticket 4471)
**App:** accounts
**Status:** Open

## Description

`GET /accounts/ACC-2/balance` returns HTTP 500. ACC-2 is a newly opened account with no
postings yet. The customer sees "something went wrong" on the balance screen.

## Acceptance criteria

- AC1: `GET /accounts/ACC-1/balance` still returns 200 with `"balance":"150.00"`.
- AC2: `GET /accounts/ACC-2/balance` returns 200 with `"balance":"0.00"`.
- AC3: `GET /accounts/NOPE/balance` still returns 404.
- The unit tests pass, and the application log shows the balance computed for ACC-2.

## Root cause

_(written by the investigate stage; not yet)_
