# BUG-103: Balance is a cent short of the ledger for a half-cent posting

**Reported by:** finance operations (reconciliation break, statement run of 2026-09-22)
**App:** accounts
**Status:** Open

## Description

ACC-3 has one posting of 10.005. The ledger team's statement shows 10.01 for it, the way
the bank rounds: a half cent rounds up. `GET /accounts/ACC-3/balance` returns
`"balance":"10.00"`, one cent short, and the daily reconciliation breaks on it.

## Acceptance criteria

- AC1: `GET /accounts/ACC-3/balance` returns 200 with `"balance":"10.01"`.
- AC2: `GET /accounts/ACC-1/balance` still returns 200 with `"balance":"150.00"`.
- AC3: `GET /accounts/NOPE/balance` still returns 404.
- The unit tests pass, and the application log shows the balance computed for ACC-3.

## Root cause

_(written by the investigate stage; not yet)_
