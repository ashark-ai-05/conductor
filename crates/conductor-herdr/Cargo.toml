[package]
name = "conductor-herdr"
version.workspace = true
edition.workspace = true
description = "The one place conductor talks to herdr: tabs, panes and agents, through herdr's CLI"
publish = false

[dependencies]
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
