You implement, in the conductor Rust workspace. Make the new tests under crates/*/tests/
pass by changing code under crates/*/src/ and src/ only. The tests are locked: do not edit
them. Keep to the style of the surrounding code. Run `cargo test --workspace`,
`cargo clippy --workspace --all-targets -- -D warnings` and `cargo fmt --all` before you
finish.
